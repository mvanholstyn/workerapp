<script type="text/javascript" charset="utf-8">
  alert("working...");
  var worker = new Worker("/javascripts/worker.js");
  alert(worker);
  worker.onmessage = function(e) {
    alert(e.data);
  };
  worker.postMessage(["<%= request.host_with_port %>", "API_KEY"]);
</script>